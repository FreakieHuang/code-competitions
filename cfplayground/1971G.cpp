#include <bits/stdc++.h>

using namespace std;


void solve() {
    int n; cin >> n;
    vector<int> an(n);
    for (int i=0; i < n; ++i) {
        int aa; cin >> aa;
        an[i] = aa;
    }
}

int main() {
    int t; cin >> t;
    while (t--) {
        solve();
    }
}